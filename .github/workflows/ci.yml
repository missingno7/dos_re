name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python: ["3.11", "3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - name: Install dependencies (numpy is a first-class core dep)
        run: pip install numpy pytest pytest-xdist
      - name: Boundary + syntax lint
        run: python tools/lint.py
      - name: Undefined-name guard
        run: python tools/check_undefined_names.py
      - name: Doc links resolve
        run: python tools/check_doc_links.py . --exclude pynuked_opl3
      - name: Test suite
        run: python -m pytest tests -q -n auto
      - name: End-to-end example (oracle -> hook -> verify -> snapshot)
        run: python examples/minimal_adapter/example.py
      - name: Full-lifecycle example (frames, demos, both oracles, state mirror)
        run: python examples/tiny_frame_game/walkthrough.py
